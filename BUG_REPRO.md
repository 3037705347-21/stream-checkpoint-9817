# 修复前故障复现（Docker）

## 项目与标准命令

模块为 `example.com/stream-checkpoint`，验证命令为 `go test -count=20 ./...`。

## 环境构建与编译

使用当前平台的 `golang:1.22` 镜像构建成功，容器内 `go build ./...` 成功。

## 故障触发步骤

在容器工作目录执行 `go test -count=20 ./...`。

## 实际错误输出

测试退出码为 1；批次标记返回 `edge-b` 在前，序列号为 12 和 8，预期为按分片排序的 11 和 7；快照还暴露了可变分片切片。

## 期望行为

检查点快照和提交批次应保持独立、稳定的有序分片与原始序列号。
